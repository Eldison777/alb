# Database
DATABASE_URL=postgresql://user:password@localhost:5432/albshift

# Admin
ADMIN_PASSWORD=change-me-to-a-strong-password

# Server
PORT=3001

# CORS (comma-separated origins)
CORS_ORIGINS=http://localhost:3000

# SMTP (optional — contact form emails)
SMTP_HOST=
SMTP_PORT=587
SMTP_USER=
SMTP_PASS=
